# Intent for `src/cost-tracker.ts`

**What changed:**
Added Gemini models (`gemini-2.5-flash` and `gemini-3`) to `MODEL_PRICING` dictionary.

**Invariants:**
- Standard Google AI Studio pricing should be applied to the models correctly.
