# Intent for `.env.example`

**What changed:**
Added `GOOGLE_API_KEY=your_google_api_key_here`.

**Invariants:**
- `GOOGLE_API_KEY` must be present to warn user they can run Gemini models independently of the IT LiteLLM proxy.
